# Frontend (React + Mantine)

A focused UI for the RAG backend:
- Create session
- Drag/drop upload
- File library
- Chat + Sources (citations)

## Run
```bash
cd frontend
npm install
npm run dev
```

## Backend URL
The backend URL is stored in browser localStorage. Default is `http://localhost:8000`.
