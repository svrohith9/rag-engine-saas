APP_HOST=0.0.0.0
APP_PORT=8000

# LLM provider
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Embeddings (optional but recommended for true semantic RAG)
GEMINI_EMBED_MODEL=text-embedding-004

# Storage
UPLOAD_DIR=./data/uploads
DB_PATH=./data/rag.sqlite3
